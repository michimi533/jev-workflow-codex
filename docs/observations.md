# 観測結果（2026-09-20）

日本語Wikipediaで、東京スカイツリー→「歴史」、東京タワー→「沿革」、東京都庁舎→「施設」と検索・移動した。情報を読み比べて回答する課題ではなく、指定した画面まで移動する課題である。

## 以前の固定手順での予備比較

|項目|通常のCodex操作|Jev併用時のCodex|
|---|---:|---:|
|経過時間|135.031秒|64.027秒|
|実操作|14|14|
|集計したCodex応答|15|2|
|入力|3,139,513|437,727|
|うちキャッシュ入力|3,132,800|435,840|
|うち非キャッシュ入力|6,713|1,887|
|出力（推論を含む）|1,034|619|
|入力＋出力|3,140,547|438,346|

Jev併用側の総数は86.0%少なかった。大部分は長い会話履歴のキャッシュ入力であり、この割合は料金・利用枠の削減率ではない。キャッシュ入力は入力の内数。準備・開発・調整・除外試行・動画作成は対象外。直前の準備応答も含めた参考値は通常3,347,871、Jev併用656,722だが、これも開発全体ではない。

通常側ではCodexが操作ごとに観測結果を読み、次を選んだ。Jev側では事前に作った14の小目標をプログラムが順に回し、各目標についてJevが操作と対象を選んだ。両方で同じ入力文字列を指定した。Jevへ14操作を一度に投げたわけではない。

各方式1回分の記録で、順序・画面情報の絞り方・事前の手順化が異なる。Jev側は後から複数の調整試行をした。通常側の途中確認・進捗説明と、双方の画像保存時間を含む。モデル単体の比較や一般的な速度保証には使わない。

Jev側はHTTP 503から1回再開し、その時間も64.027秒に含む。成功した14判断のAPI応答時間合計は8.413秒。返却された費用合計は$0だが、全請求を照合した値ではない。この試行ではJev APIのトークンusageを保存しておらず、トークンは不明。

事前の失敗には、正しい節リンクが40候補の外に落ちたケース、503での停止、記録関数が古い記録先を参照したケースがある。候補を80へ増やし、記録先を引数で渡して測定した。条件変更と記録不備があるため、成功率の統計値は算出しない。

## その後の汎用エンジン検証（上表とは別試行）

- 15段階を検証し、14操作で完了。開始時に開いていた検索欄は再度開かず省略。
- 3回の区切り停止から同じチェックポイントで再開した。
- Jev要求17回、入力17,356、出力2,503、計19,859トークン。
- 17回にはdry-run1回、区切りに達して操作を見送った判断2回を含む。
- 保存時間105.069秒にはdry-runとCodexの呼び出し間隔が含まれる。上表との速度比較に使用しない。
- この試行のCodex側トークンは未集計。前の比較で欠けたJevトークンを、この数値で補完しない。

最初の試行では検索欄がcombo boxだったため停止した。認識を直して手順版を2へ更新し、新しい実行としてやり直した。この失敗分も合わせた開発中のJev要求は19回、22,015トークン。返却費用は$0。期間限定料金下の結果で、将来の料金を示さない。

同一手順のneeds_codexからの復帰は模擬テスト、pausedからの再開は実画面で確認。最終の費用上限・データ検査・Retry-After追加は模擬テストで確認し、最終モジュールのブラウザ内読み込みも確認した。noteへの書き込みは試していない。

## 公開資料の範囲

集計値のみを公開している。元のCodex会話ログ、認証情報、個人のファイルパス、応答IDは含めない。手元の保存済み記録から作った観測報告であり、このリポジトリだけで過去の会話トークンを独立に再集計できる資料ではない。
